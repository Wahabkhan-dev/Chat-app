'use client';

import React from 'react';

// Sessions never expire automatically — only explicit logout ends a session.
// This component is intentionally disabled.
export const SessionExpiryWarning: React.FC = () => null;
